{erl_opts, [debug_info]}.

{deps, [{parserl, {git, "https://github.com/williamthome/parserl.git",
                  {branch, "main"}}}]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [eel]}
]}.

{edoc_opts, [{doclet, edoc_doclet_chunks},
             {layout, edoc_layout_chunks},
             {preprocess, true},
             {dir, "_build/docs/lib/eel/doc"}]}.

{profiles, [
    {test, [
        {extra_src_dirs, [
            {"test/support", [{recursive, true}]}
        ]}
    ]}
]}.

